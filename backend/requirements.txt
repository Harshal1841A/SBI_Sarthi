fastapi==0.111.0
uvicorn[standard]==0.30.0
websockets==12.0
pydantic==2.7.4
python-multipart==0.0.9
langgraph==0.2.62
langgraph-checkpoint-sqlite<3.0.0,>=2.0.0
langgraph-checkpoint-postgres<3.0.0,>=1.0.0
psycopg[binary]>=3.1.0
python-jose[cryptography]==3.3.0
python-dotenv==1.0.1
httpx==0.27.0
openai>=1.30.0
numpy==1.26.4
structlog==24.4.0
prometheus-client==0.20.0
aiofiles==24.1.0
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0
black==24.4.2
mypy==1.10.0
sqlalchemy==2.0.31
aiosqlite==0.20.0
bcrypt==4.1.3
passlib==1.7.4
asyncpg==0.29.0
redis==5.0.4

# ── Moved to requirements-optional.txt ──────────────────────────────────────
# silero-vad: pulls ~2 GB PyTorch — causes HF Spaces build timeouts.
#   vad.py falls back to a lightweight energy-based VAD when this is absent.
#   Install only when running the full voice pipeline: pip install silero-vad==5.1.2
#
# locust: load-testing tool, not needed at runtime.
#   Install for perf tests only: pip install locust==2.28.0
